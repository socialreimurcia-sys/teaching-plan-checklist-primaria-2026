[build]
  functions = "netlify/functions"
  command = "npm install && cp fustersaura_primary_checklist.html index.html"

[functions."*"]
  timeout = 26

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
